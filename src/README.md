# src/ — Application Source Code

All PortFlow AI application code lives under this directory.

## Expected Structure

```
src/
├── frontend/     # React + Vite + TypeScript application  (Plan 5)
└── backend/      # Python FastAPI application             (Plan 2)
```

Application code has not been implemented yet.  See:
- `docs/setup-guide.md` for the intended directory layout and run commands.
- `docs/AI_HANDOFF.md` for the next task (Plan 2 — Backend scaffold).
